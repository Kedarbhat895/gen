[packages]
openai = "==0.27.8"
python-dotenv = "==1.0.0"
langchain = "==0.0.11"  # Lock the version to avoid deprecated imports and issues
pydantic = ">=1.10,<2.0"  # Use Pydantic v1 for backward compatibility with LangChain

[requires]
python_version = "3.11"